# Direct release inputs used to keep GOVP release jobs on a reviewed toolchain.
# Transitive resolution is recorded by the release logs and must be reviewed
# before each tag; this file is not a cross-platform hash lock.
build==1.5.0
certifi==2026.7.22
cryptography==50.0.0
hatchling==1.31.0
pyinstaller==6.21.0
pyinstaller-hooks-contrib==2026.6
pytest==9.1.1
twine==7.0.0
